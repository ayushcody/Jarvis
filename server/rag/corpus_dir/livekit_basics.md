# LiveKit Quick Notes
- LiveKit provides rooms, participants, tracks (audio/video), and realtime text/byte streams.
- Use text streams for chat/LLM tokens and byte streams for arbitrary binary payloads (like TTS audio).
- Clients and servers use JWT access tokens to join rooms.
